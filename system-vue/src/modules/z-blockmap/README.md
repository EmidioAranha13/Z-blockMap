# Z-BlockMap

Módulo de construção de mapas em pixel (tiles).

O editor, o canvas e as ferramentas ficam em `src/pages/MapEditorPage.vue`,
`src/composables/useMapEditor.js` e `src/components/`.

Os arquivos salvos vão para `maps/pixelMap/` (`JSONS`, `PREVIA`, `IMGS`) na raiz do repositório.
